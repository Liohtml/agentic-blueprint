# Agent Configuration: {{AGENT_NAME}}

## Role
{{ROLE}} — see [{{AGENT_FILE}}](blueprint/agents/{{AGENT_FILE}})

## Assigned Directories
{{#DIRECTORIES}}
- `{{PATH}}/`
{{/DIRECTORIES}}

## Shared Directories (read-only in Phase 2)
{{#SHARED}}
- `{{PATH}}/`
{{/SHARED}}

## Active Phase
> Updated per phase.

Phase: **{{PHASE_NO}}**
Read: [{{PHASE_FILE}}](blueprint/phases/{{PHASE_FILE}})

## Working Rules
1. Work ONLY in your assigned directories
2. Shared directories are read-only in Phase 2
3. One Chunk per thread, fresh context window
4. Stick to the loop limits
5. Escalate instead of looping endlessly
6. Never review yourself
7. Never merge without Human Go

## Branch Format
`feature/{{CHUNK_NO}}-{{AGENT_SHORTNAME}}-{{DESCRIPTION}}`
